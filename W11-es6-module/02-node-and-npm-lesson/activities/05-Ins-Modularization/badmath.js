const pie = 'apple';

const predictable = () => 1;

// module.exports is an object we use to store variables or methods
module.exports = {
  pie,
  predictable,
};

// module.exports is an object we use to store variables or methods. It allows us to export multiple things from a file. It is important because it allows us to use the same file in multiple places without rewriting the code in each file. It also allows us to use the same file in multiple places without rewriting the code in each file. 